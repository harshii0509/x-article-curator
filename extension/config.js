/**
 * Extension config — change these when shipping or rebranding.
 * Default API URL is used when the user has not set one in Options.
 */
window.NIGHTSTAND_CONFIG = {
  DEFAULT_API_URL: "https://nightstand.com",
  // OAuth client must have this extension's redirect URI in Authorized redirect URIs.
  GOOGLE_CLIENT_ID:
    "443865035562-frma26noeasdm8udf075r4kad2oddskc.apps.googleusercontent.com",
};
